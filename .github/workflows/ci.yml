name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  # Allow re-running manually from the Actions tab when triaging flakes.
  workflow_dispatch: {}

# Cancel any in-progress run for the same ref when a new commit lands — saves
# minutes on busy PRs.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Typecheck + lint + build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        # `npm ci` is strict — it fails if package.json and package-lock.json
        # have drifted, which is exactly the contract we want for CI.
        run: npm ci

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Lint
        run: npx eslint "src/**/*.{ts,tsx}"

      - name: Test
        # node:test suite via tsx — mostly pure-logic invariants (loyalty/points
        # math, combo-deal savings, delivery thresholds, recipe base-slug, cohort
        # retention + CLTV) plus a happy-dom render regression test for the inline
        # BundleComposer (guards the mount → fetch → init flow that shipped two
        # stuck-loading / lost-prefill bugs). Runs before build so a broken
        # invariant fails the PR loudly rather than shipping silently.
        run: npm test

      - name: Build
        # Build catches issues tsc + lint can miss (Next route validation,
        # bundler errors, server-only imports in client components). Runs
        # without a Sentry DSN — `next.config.ts` is designed to no-op
        # cleanly in that mode.
        run: npm run build
        env:
          # Anything secret stays out of CI for now; production env is set on
          # Vercel. CI only validates that the code compiles + boots.
          ADMIN_PASSWORD: ci-placeholder
