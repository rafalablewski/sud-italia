# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts.bak

# iOS (bare RN): the source IS committed (ios/project.yml, ios/Ottaviano/*,
# ios/OttavianoKDS/*, ios/Podfile, ios/ci_scripts/*). Only generated artifacts
# are ignored — the XcodeGen-produced project, the pod-install workspace + Pods,
# and build output.
ios/Ottaviano.xcodeproj/
ios/Ottaviano.xcworkspace/
ios/Pods/
ios/Podfile.lock
ios/build/
ios/DerivedData/
ios/main.jsbundle
ios/.xcode.env.local

# Android (not yet set up natively)
/android

# Metro / Babel caches
.metro-health-check*
*.tsbuildinfo

# Env / secrets
.env*.local

# macOS
.DS_Store
