# /tests/ — drafts, sketches and design R&D

Scratchpad for things that aren't part of the deployed site. Anything in this folder is for review, exploration or comparison and should not be linked from the production app.

## Convention

- All draft sketches, wireframes and design R&D go under `/tests/sketches/<name>.html` (or `.svg`).
- Standalone exploratory pages (logo concepts, palette tests, type specimens, competitor comparisons) live at the top of `/tests/`.
- Nothing in `/tests/` ships.
- If a sketch becomes a real component, port it to where it belongs in `src/` and delete the draft.

## Current sketches

- `sketches/ottaviano-kds-shell-preview.html` — **static, JS-free preview of the OttavianoKDS (operator native app) shell** after the "shell polish + universal surface search" wave. SwiftUI can't render in the web container, so this mirrors the Swift in the browser (all markup is static SVG-`<symbol>` + CSS — renders in any viewer, even one that strips scripts): palette = the **generated** web Core dark tokens carried into Swift (`native/ottaviano-ios/Sources/DesignSystem/Tokens.generated.swift` ← `src/app/themes/core/tokens.css`), IA = the full 54-surface `OPERATOR_NAV` (`docs/native/parity/PARITY-LEDGER.md`) with the branded identity header, the search bar and `OperatorNavRow` (icon chip + scaffold wrench). The right column stacks worked detail surfaces — Dashboard, Customers, Stock, Feedback, a KDS ticket, a live surface card and a scaffold card. Open directly in a browser. Figures illustrative; nothing in `/tests/` ships.
- `sketches/ottaviano-kds-wave-b-analytics-explainers.html` — **static, JS-free preview of Wave B (analytics + the five-section ⓘ explainer)** for the OttavianoKDS operator app. Shows Reports KPIs each with an ⓘ, a 14-day revenue bar chart, a fulfilment-mix ring, ranked top-sellers, and the open `MetricExplainer` sheet rendering the five required Rule #12 sections in fixed order/labels. Mirrors `src/admin-v3/ui/Explainer.tsx`; charts are hand-rolled (no Swift Charts dependency). Same generated Core dark tokens. Open directly in a browser.
- `sketches/ottaviano-kds-wave-a-detail-sheets.html` — **static, JS-free preview of Wave A (rich rows + detail drill-in sheets)** for the OttavianoKDS operator app. Shows the Admin list row before (flat, no drill-in) vs. after (avatar · VIP/recency · points · chevron → sheet), then the Customer profile, Purchase-order and Staff detail sheets. Same generated Core dark tokens. Note: the Swift implementation renders **only** fields present on each `/api/v1` DTO (Rule #1) — the preview's recent-orders / PO-lines / shifts are illustrative of the layout and call out facade gaps. Open directly in a browser.
- `sketches/ottaviano-kds-shell-before-after.html` — companion (also static/JS-free) showing **what the wave changed**: the operator rail before (bare `List { Label }`) vs. after (identity header · search · icon-chip rows), then the new shell pieces in isolation (nav-row states, DSBadge tones, KPI tile + stat chip, search empty state). Same generated Core dark tokens. Open directly in a browser.
- `sketches/login-v3-a-operator-terminal.html`, `sketches/login-v3-b-split-console.html`, `sketches/login-v3-c-spotlight-minimal.html` — **three directions explored for the login door redesign** (`/login` + `/admin/login`). All three move login onto the live admin design (v3 "Operator Terminal") — dark canvas, crimson brand + platinum accent, Fraunces/Inter/JetBrains-Mono, av3 tokens lifted 1:1 from `src/app/themes/admin-v3/index.css`. **C — Spotlight Minimal is the chosen / shipped direction** (now updated to carry **B's brand-lockup header** and centered on both axes): no card chrome, a centered column under one spotlight beam + a slow scanline, framed by a thin platinum bracket, with underline-only inputs — it mirrors the live `LoginForm.tsx` + `.av3-auth*` (§23). **A — Operator Terminal** (centered glass-blur card, masked grid + brand glow, brand→platinum hairline) and **B — Split Console** (two-panel cockpit: left brand panel with ambient aurora + live status ticker, right sign-in column) are kept as the rejected alternatives for reference. Open each directly in a browser. Static mockups — figures illustrative.
- `sketches/admin-v3-form-controls.html` — preview of the v3 admin form controls after the styling pass (buttons, selects with painted chevron, restyled native checkboxes, radios, inputs, switches, chips) rendered in both dark and light admin themes. Tokens + rules mirror `src/app/themes/admin-v3/index.css`. Open directly in a browser.
- `sketches/admin-v3-surfaces.html` — preview of the v3 admin "heavy" surfaces after the styling pass (KPI tiles with accent rail + hover lift, the data Table with sticky-header shadow + smooth row hover, and the Dialog entry animation) in both themes. Tokens + rules mirror `src/app/themes/admin-v3/index.css`. Open directly in a browser.
- `sketches/admin-v3-shell-states.html` — preview of the v3 admin shell/state polish (sidebar nav active state, kanban lane + order cards, empty-state icon chip, and skeleton loaders) in both themes. Tokens + rules mirror `src/app/themes/admin-v3/index.css`. Open directly in a browser.
- `sketches/admin-v3-charts.html` — preview of the v3 chart interactivity (AreaChart follow tooltip + y-axis labels, BarChart hover-to-focus, Donut segment hover/tooltips) in both themes. Mirrors `ui/Chart.tsx` + the `.av3-chart*` CSS. Open directly in a browser.
- `sketches/operations-interactivity-pass.html` — visual mockup of the **Operations interactivity pass** across all five Operations pages. The three log pages (**HACCP / Waste / Shift handover**) show the KPI rail with clickable five-section ⓘ explainers, the **live form feedback** (HACCP safe-range gauge that marks the typed temp, Waste live entry preview with projected daily write-off, Handover live sign-off summary that warn-tints on an unclear check), search + filter chips, a Board⇄Table toggle, and clickable cards/rows → **detail popups**. The **Menu** and **Recipes** editors show the two-column drill-in dialog with its live preview panel (Menu: live customer menu-card; Recipes: food-cost gauge + margin readout + per-portion macros). Page switcher, ⓘ dialogs, view toggle and popups are clickable. Tokens mirror `src/app/themes/admin-v3/index.css`; figures are illustrative. Open directly in a browser.
- `sketches/growth-workbenches-upsell-crosssell.html` — visual mockup of the **Upsell (Bundles)** and **Cross-sell (Combos)** workbenches added to admin-v3 — the Menu/Recipes-style interactivity pass: reactive KPI rail with five-section ⓘ explainers, search + Board⇄Table view toggle, board cards with live per-item stats, and a two-column drill-in editor with a live customer preview (bundle card + 30d performance grid; combo nudge + a real-price worked example). Tabs / view toggle / ⓘ dialog are clickable. Tokens mirror `src/app/themes/admin-v3/index.css`; figures are illustrative (the real surfaces read `/api/admin/bundle-analytics`, the live config and the clock). Open directly in a browser.
- `sketches/boardroom-ai-team.html` — visual mockup of the **AI Boardroom** (`/admin-v3/boardroom`, the CEO/COO/CFO/CMO console). Tabbed: Overview (traffic-light KPI rail + agent-status cards + quick actions + attention flags), Finance · CFO persona panel (owned KPIs + a Claude chat with an `update_item_price` approval card), and Meetings (a weekly-review transcript with avatar rows + a decisions list). Tokens mirror `src/app/themes/admin-v3/index.css`; data is illustrative (the real page reads the live store). Open directly in a browser.
- `sketches/agent-hq.html` — visual mockup of **Agent HQ** (`/admin/agent-hq`, the Boardroom rebuilt as the editable agent-fleet console). Six clickable sections — **Command center** (model picker, two KPI rails, the 9-agent roster with status/model/authority/spend chips, an org/reporting chart, attention flags, convene-the-board), **Scorecards** (per-agent live KPIs + authored targets), **Work** (schedules + cross-agent activity feed), **Approvals** (gated actions with Action / Mark done / Dismiss), **Inbox** (escalation panel + per-agent chat with a tool card), **Reports** (briefing transcript + decisions). The **agent editor** modal exposes every field (name, role, status, reports-to, model, effort, authority, runtime memory, mandate, responsibilities, KPIs, guardrails, escalation, tone, collaborators, tools with ·writes badges, spend caps, schedule) and its **Live system prompt** tab regenerates as you type. Tokens approximate `src/app/themes/admin-v3/index.css`; data is illustrative. Open directly in a browser.
- `sketches/agent-hq-1-mission-control.html` … `-5-analyst.html` — **five Agent HQ redesign directions** (generated from `sketches/agent-hq-redesign/gen.mjs`, which inlines the real av3 tokens 1:1 + Inter/Fraunces/JetBrains-Mono): **1 Mission Control** (single-screen cockpit: persistent agent rail · centre command · "needs you" right column), **2 Org-first** (the reporting tree is the navigator; a node opens its scorecard + charter beside it), **3 Workflow Pipeline** (Backlog→Queued→Running→Review→Done kanban with agents as assignees; approvals are a column), **4 Card Gallery** (people-first big agent cards with a success ring + quick actions), **5 Analyst Dashboard** (exec/reporting view: cost + success gauges, runs/cost trends, KPI board, feed sidebar). Static; regenerate with `node gen.mjs`. Open each directly in a browser.
- `sketches/agent-hq-redesign-v2.html` — **round 2: 5 more Agent HQ directions in ONE file**, this time with the **real** `src/app/themes/admin-v3/index.css` inlined verbatim and markup built from the actual `.av3-*` classes (pixel-faithful, not a reconstruction). A top switcher flips between: **6 Console** (master–detail + sub-tabs), **7 Activity Stream** (chronological feed + needs-you rail), **8 Daily Brief** (reading column: headline / attention / decisions / desk), **9 Bento Grid** (asymmetric tile dashboard), **10 Fleet Table** (dense data-grid of every agent). Generated by `sketches/agent-hq-redesign/gen2.mjs` (`node gen2.mjs`). Open directly in a browser.
- `sketches/agent-hq-console.html`, `sketches/agent-hq-daily-brief.html`, `sketches/agent-hq-bento.html` — **full builds of the three chosen directions** (real `themes/admin-v3/index.css` inlined; actual `.av3-*` classes). **Console** is fully interactive — click an agent on the left and the Overview / Charter / Scorecard / Timeline / Chat sub-tabs on the right (browser-side render from embedded data, incl. live-prompt + target-vs-actual logging UI). **Daily Brief** is a reading-first morning brief (pulse KPIs, attention flags, escalation, board decisions, the desk's one-liners, runs/cost). **Bento** is an asymmetric tile dashboard (sales hero + chart, success gauge, cost, needs-you, cost&quality, fleet, activity, upcoming work, scheduled). Generated by `sketches/agent-hq-redesign/gen3.mjs` (`node gen3.mjs`). Open directly in a browser.
- `sketches/agent-hq-full.html` — **the whole Agent HQ, all 8 tabs polished to the Console standard, in ONE file** (real `themes/admin-v3/index.css` inlined; actual `.av3-*` classes). Top chips switch tabs: **Command center** (fleet KPIs + sales/cost rails + org/activity/recent/upcoming/digest/cost grid), **Agents** (the interactive Console — pick an agent, switch Overview/Charter/Scorecard/Timeline/Chat), **Scorecards** (per-agent cards with success bar + tiles + target-vs-actual), **Work** (assign + drop-targets + Unassigned/Queued/Recent), **Approvals**, **Inbox** (escalations + list + chat), **Reports** (transcript + decisions + CSV/PDF), **Settings** (model + budget + automation). Generated by `sketches/agent-hq-redesign/gen4.mjs` (+ `_shared.mjs`). Open directly in a browser.
- `sketches/subpage-hero/` — design R&D for the **one shared subpage hero** (title + location filter + search + segmented filters + actions, unified across all admin pages). `index.html` is an interactive mockup (real dark tokens + fonts, clickable variant switcher, shown in a faux page context). `gen.mjs` renders the same 5 candidates to PNG via `@resvg/resvg-js` (`npm i -D @resvg/resvg-js`, then `node gen.mjs`).
- `sketches/core-unified-shell.html` — interactive mockup of the **unified Core shell** (`CoreShell`): no sidebar, one shared two-row header (brand + the POS·KDS·Guest·Service `CoreNav` + global actions / eyebrow + the surface's viewnav + its own controls). Both the primary nav **and** each surface's sub-views are clickable — 12 toggleable pages: POS (Order · Tender), KDS (Fleet · Floor · Chef, dark wall), Guest (Inbox · Guests · Loyalty · Concierge · Book), Service (Floor · Slots). The header bits (eyebrow, sub-nav, controls) reflow per page. **Pure HTML/CSS — no JavaScript** (switching is the hidden-radio `:checked` technique), so it renders and toggles even in a sandboxed/script-blocked preview. Tokens lifted 1:1 from `src/app/themes/core/suite.css`; data is illustrative. Open directly in a browser.
- `sketches/core-all-functionalities.html` — the **full Core feature showcase**: the same 12-page unified shell as `core-unified-shell.html`, but every surface is **populated with realistic data** and each pane carries an intro banner + ⓘ caption calling out its functionality, so the whole feature set reads in one file. Covers POS (multi-tab checks, pace steering, role/pace menu badges, dine-in coursing with per-course Fire, combo + cross-sell offers, kbd-hint; Tender = Charge dialog + split/partial keypad), KDS (Fleet KPIs + cross-truck benchmark + per-truck pace bars & ticket previews; Floor's New/Firing/Ready lanes with SLA tiers + cook-meters; Chef's oversized station make-queue), Guest (WhatsApp Inbox with bot/staff thread, CRM roster + profile drawer, Loyalty tiers + family wallets, Concierge capability toggles + live-test JSON + EU-14 matrix, Book slot+table), Service (Floor twin with live bottleneck banner + predictive seating, Slots capacity + demand surge). **Pure HTML/CSS — no JavaScript** (hidden-radio `:checked`); Charge→Tender is wired. Tokens lifted 1:1 from `src/app/themes/core/suite.css`; figures are illustrative. Open directly in a browser.
- `sketches/core-loading-skeletons.html` — **before / after** for the Core loading states: the bare centered `pane-msg` "Loading…" label vs the new **shimmer skeleton** shaped like the content arriving (CRM table, Inbox conversation list with avatars, transcript bubbles). Mirrors the `.core-suite .skel*` rules in `src/app/themes/core/suite.css` + the shared `src/core/shared/Skeleton.tsx` primitive (`Skeleton` / `SkeletonTable` / `SkeletonList`) used across CRM · Loyalty · Inbox · Slots · Book. The sheen sweep stops under `prefers-reduced-motion`. Open directly in a browser.
- `sketches/core-v2-design-language.html` — **clean-room design-language proposal for Core v2** (the literal `/core-v2/` parallel rebuild). A deliberately fresh direction, distinct from today's `/core` (`suite.css`): **light-first** warm-porcelain canvas with a **true dark mode** (toggle top-right; KDS will default dark for the kitchen wall), an **all-sans grotesk** display (Bricolage Grotesque — drops the Fraunces serif), **flat materials** (1px hairlines, crisp shadows, no glass / gradient / glow → reinforces Core ≠ Admin), the refined Neapolitan burgundy brand, and a new shell (top command bar + segmented surface switcher + context subbar). Shows the **POS** surface as the hero (category rail · text-forward menu cards · multi-tab ticket with coursing + Fire + cross-sell offer + Charge CTA) on real menu names/prices. These tokens become `themes/core-v2/` once the direction is locked. Interactive theme toggle + live clock. Open directly in a browser.
- `sketches/core-v2-all-surfaces.html` — **all four Core v2 surfaces in one switchable file** (POS · KDS · Guest · Service), the clean-room `/core-v2/` design language made live. **Dark-default** with a light toggle (KDS stays dark — the kitchen wall). Click the top segmented switcher to flip surfaces; each surface's subbar (eyebrow + view tabs + controls) reflows. Covers POS (rail · menu cards · multi-tab ticket + coursing + Charge), KDS Floor (New→Firing→Ready lanes, SLA tiers, cook-meters, bump), Guest Inbox (WhatsApp 3-pane + live order context + tier + NBA), Service Floor (zoned table tiles + tonight's capacity/slots with demand surge). Real menu names/prices; tokens are the future `themes/core-v2/`. See `docs/design-system/core-v2/`. Open directly in a browser.
- `sketches/core-v2-pos-live-snapshot.html` — a **static snapshot of the real `/core-v2/pos` build** (not a hand-drawn mockup): the actual SSR HTML with the actual `themes/core-v2/` CSS inlined 1:1 and scripts stripped. Proves the live scaffold renders the design language with **real per-location menu data** (9 pizza cards, all six categories in the rail, the no-open-check ticket state), dark-default. Interactive bits (clock, category switch, theme toggle) are inert in the snapshot — they work in the live app. Regenerate by re-running the snapshot step against a logged-in dev server.
- `sketches/core-v2-pos-ticket.html` — **Core v2 · POS, the wired till** (preview). Renders with the **exact** `themes/core-v2` CSS, showing an open dine-in check: multi-tab rail, channel + covers, two courses (Starters fired ✓, Mains with a Fire button), `cv-qstep` qty counters, a cross-sell offer, a combo-discount row, totals, and Send/Charge. The live order flow (create → add → coursing/Fire → combo discount → Charge → Tender) is wired 1:1 to `/api/admin/pos/*` and verified end-to-end; the ticket is client-rendered so it doesn't appear in a bare SSR snapshot, hence this preview. Open directly in a browser.
- `sketches/core-v2-kds-floor.html` — **Core v2 · KDS Floor, the kitchen wall** (preview). Renders with the **exact** `themes/core-v2` CSS (`.cv-kds`, always-dark even in light mode): the 6-up KPI band, the station strip, and three lanes (New → Firing → Ready·Expo) of `.cv-tk` ticket cards with SLA **tones** (firing / warn / risk / late / ready) driving the left border, due clock, cook-meter fill, and bump button. The wall is wired 1:1 to the live order stream (`useAdminOrdersStream` → `analyzeTruck` → `buildKdsTicket` → `groupTicketsByColumn`) and bump = `PUT /api/admin/orders` (verified); tickets are client-streamed so this is a populated preview. Open directly in a browser.

- `sketches/core-v2-guest-inbox.html` — **Core v2 · Guest · Inbox** (preview). The WhatsApp till as a 3-pane console (conversation list with LIVE/PAY badges + filters · actor-toned message bubbles + composer · live order + guest/tier context) over a 5-up KPI strip, in the real `cv-` CSS. Wired 1:1 to `/api/admin/whatsapp/*` (sessions+transcripts+flags+metrics, send, archive/pin); the seed has no WhatsApp data so the live inbox shows its empty state — this preview is populated. Open directly in a browser.

- `sketches/core-v2-guest-crm.html` — **Core v2 · Guests / CRM** (preview, light mode). The customer book: a roster table with segment chips + sort + an RFM-derived Health column over a KPI strip; the profile drawer adds a stat grid, consent toggles, recent-orders timeline, points adjust and notes. Real `cv-` CSS, wired to `/api/admin/crm` (+ notes/points/consent). Open directly in a browser.

- `sketches/core-v2-guest-loyalty.html` — **Core v2 · Loyalty** (preview). Four tabs (Members · Wallets · Redemptions · Win-back) over a KPI strip; Members table with tier badges + filter + sort, row → points-adjust. Real `cv-` CSS, wired to `/api/admin/{members,wallets,wallet-redemptions,retention}`. Open directly in a browser.

- `sketches/core-v2-guest-concierge.html` — **Core v2 · Concierge** (preview). A two-pane MCP inspector: six capabilities with live exposure toggles · the selected capability's sample JSON + a Test link + the EU-14 allergen matrix per location. Real `cv-` CSS, wired to `/api/admin/concierge` + `/api/agent/*`. Open directly in a browser.

- `sketches/core-v2-guest-book.html` — **Core v2 · Book** (preview). Slot + table in one move: dine-in slot picks, party stepper, table picks with live fit/conflict (booked/too-small dim) + ✨ Recommend, guest fields + override, and today's bookings with cancel. Real `cv-` CSS, wired to `/api/admin/{slots,floor/tables,floor/reservations,booking}`. Open directly in a browser.

- `sketches/core-v2-service-floor.html` — **Core v2 · Service · Floor** (preview). The live room: a KPI strip + kitchen-bottleneck banner over zoned table tiles toned by state (free / seated / freeing / reserved / out-of-service) with party / dwell / open check; tap to seat/clear. Real `cv-` CSS, wired to `/api/admin/floor-twin`. Open directly in a browser.
- `sketches/core-v2-service-slots.html` — **Core v2 · Service · Slots** (preview, Demand tab). The Demand Exchange: per-slot tier (under/healthy/tight/over/kitchen-capped) + recommended lever (raise/trim/protect/hold) with Apply / Apply-all; the Manage tab carries capacity fill bars. Real `cv-` CSS, wired to `/api/admin/{slots,demand-exchange}`. Open directly in a browser.
- `sketches/comms-daily-routines-todo.html` — **interactive mockup of the comms "daily routines + full to-do lifecycle"** feature (branch `claude/todo-list-add-feature-2tjt9k`). Two columns: the **teammate portal** (`PortalInbox`) with the Notifications inbox, the new **Daily routine** checklist (team + personal items, live "x/y done today · resets daily" counter, tick to toggle), and the new **to-do lifecycle** — To-do / Done / Archived / Deleted tabs with per-row Done · Archive · Delete · Reopen · Restore (and Delete-forever on self-added items in Deleted); and the **manager board** (`CommsV3` Tasks view) with the New-daily-routine card, the routines list with Active switches, and All-tasks showing the four statuses. Dark/Light toggle top-right; tokens lifted 1:1 from `src/app/themes/admin-v3/index.css`; sample data, interactions are local to the page. Open directly in a browser.
- `sketches/welcome-brief/` — **five directions for a full-screen CEO "morning brief" landing** (`/admin/welcome` reimagined): no sidebar, no nav — the catch-up surface you open first thing, pulling yesterday's numbers, the AI boardroom's overnight decisions, what-needs-you, reports + today's outlook into one screen. `index.html` is a gallery linking all five: **1 The Front Page** (printed-newspaper editorial — masthead + lead story + per-exec op-ed columns, serif/paper), **2 Mission Control** (dark bento terminal — hero number + sparkline, KPI tiles, board decisions, live alerts), **3 Chief of Staff** (narrative — your AI chief of staff briefs you in plain English, one calm column + a to-do), **4 Executive Bento** (accent-coloured modular tile wall, Linear/Notion energy), **5 Split Hero** (fixed left hero + scrollable right feed grouped Yesterday / Boardroom / Needs you / Today). Same sample data across all five (Sud Italia, Kraków + Warszawa, zł) so the comparison is about shape. Static mockups; figures illustrative. Open `index.html` (or each file) directly in a browser.

- `sketches/service-os-*.html` — **Service OS redesign, "Liquid Glass" (2026) visual language.** Translucent frosted-glass materials (backdrop-blur + saturate), an ambient aurora backdrop the glass refracts, specular rim-light on edges, glowing ember/basil/amber/danger accents, floating detached components, and spring motion. Supersedes the first flat round per the decision log in `docs/design-system/core/redesign/PROGRESS.md`. Tokens extend `src/app/themes/core/tokens.css` with a glass layer; figures illustrative.
- `sketches/service-os-canvas.html` — **the Service OS redesign hero**: the unified **Service Canvas** (one surface, four swappable lenses + a persistent Context Dock), the IA proposed in `docs/design-system/core/redesign/`. A thin Command Bar (location scope · ⌘K universal search · live pressure indicator) sits over a left **Lens Rail** that flips between **Floor** (Twin-driven table tiles that show the single most urgent fact, with a radial action bloom on the selected table), **Line** (POS: ★-Popular-first category rail, product grid, 86'd items greyed), **Pass** (compact KDS columns with SLA meters + held-course dimming), and **Book** (tables × hours timeline with live red-hatch conflict). The **Context Dock** stays docked on every lens — the selected check (T10) with a live **course spine** (Antipasti ✓ · Primi firing · Secondi ○ · Dolci ○), fire-moment cross-sell chip, total, Fire + Pay. Lens switching is pure CSS (hidden-radio `:checked`), so it renders and toggles even where scripts are stripped. Tokens lifted 1:1 from `src/app/themes/core/tokens.css`; figures illustrative. Open directly in a browser.
- `sketches/service-os-pass.html` — **Service OS · Pass (KDS kitchen wall)**: the full-bleed dark kiosk. A KPI band (on-the-line / at-risk / longest / bumped-per-hr) + station segmented filter + a **RISK tier chip** over four lanes (New → Preparing → Ready·Expo → Just-bumped/recall). Each ticket card is a whole-card 1-tap bump target with an **SLA heartbeat** (green→amber→red) driving the left border + clock, an oversized **allergy** row, held-course `⊘` dimming, and channel tags. The Preparing lane is `hot` (pulsing rail) to show pressure-adaptive density. Tokens mirror `src/app/themes/core/tokens.css`; figures illustrative. Open directly in a browser.
- `sketches/service-os-tender-guest.html` — **Service OS · Tender + Guest sync**: the same check rendered two ways. Left, the **staff tender sheet** (rises from the Context Dock): split presets (Whole · ÷2 · ÷3 · ÷4 · By item · By seat), per-split method chips, a **quick-tender pad** with auto-change, tip presets, and an in-sheet **manager comp** flow with the `refund-guard` per-shift cap shown live + an inline **manager-PIN gate** that appears only when a comp breaches the cap. Right, the **guest phone** mirroring the exact same order over one SSE stream (live status track, "just added" round, the same kitchen timer the line sees, split-&-pay-on-phone, settled-with-tip toast). Illustrates Flows D–F. Tokens mirror `src/app/themes/core/tokens.css`; figures illustrative. Open directly in a browser.

- `sketches/service-os-responsive.html` — **web ⇄ mobile-app parity showcase** for the Service OS. Loads the same three sketch files inside a **desktop browser frame** and a **phone frame** side-by-side (via `<iframe>`), with a surface switcher (Canvas · Pass · Tender). Proves the responsive reflow: on phone the lens rail becomes a bottom tab bar, the Context Dock becomes a bottom sheet, floor tiles go 4-up→2-up, KDS lanes become a swipeable carousel — one component set, not two codebases. Open directly in a browser. (Because it embeds the sibling files, open it from the `sketches/` folder so the relative `src` paths resolve.)

- `sketches/service-os-order-detail.html` — **Order &amp; item detail** (Liquid Glass, responsive). The item-modification surface: modifier groups with min/max rules + priceDelta chips (KDS-flagged options tagged), special-request notes with quick chips, EU-14 allergen flags (shown large on KDS), course + seat assignment, qty stepper, and the server-recomputed price. A side column shows the long-press **comp / void / discount** menu with the live comp-guard (per-shift cap + PIN-gate note). Open directly in a browser.
- `sketches/service-os-guest-journey.html` — **Guest ordering journey** (Liquid Glass, responsive). Six phone frames as a filmstrip: scan QR → browse menu → customize (same modifiers/allergens staff see) → cart (with cross-sell) → pay/split (Apple Pay · card · BLIK · Bitcoin) → live status track. Reinforces that every guest action writes to the one shared check over the same SSE stream. Open directly in a browser.
- `sketches/service-os-service-handoff.html` — **Service &amp; handoffs** (Liquid Glass, responsive). Three panels: a **timing-alert center** (urgency-ranked, each with the next best action), an **expo → runner** handoff board (assign a runner/driver in one tap), and a one-tap **shift handover** auto-filled from live state (open tables, at-risk tickets, comps vs cap, cash variance, 86'd items, flagged tables + free-text note). Open directly in a browser.
- `sketches/service-os-booking-delivery.html` — **Booking &amp; delivery** (Liquid Glass, responsive). Left: **new reservation** in one move — slot picker (capacity/tight/full), party stepper (with min-spend), table pick with live conflict hatch + ✨ fit-recommend, guest fields. Right: **delivery dispatch** — kitchen/ready/on-road counts, order cards pulled from the pass, one-tap driver assign with live ETAs + auto-assign-nearest. Open directly in a browser.
- `sketches/core-redesign-0{1..5}-*.html` — **five design directions for a round of Core-shell fixes** (feedback on the live `/core` "Command" terminal chrome). Every mockup implements the SAME six fixes — (1) the POS category rail becomes **pure icon-only** (collapsed, count badges, label on hover), (2) the left Lens Rail **expands on click** (a pin toggle), **not on hover**, (3) the KDS KPI band is rebuilt on **one shared "stat strip"** component reused verbatim in POS + Service, (4) **Floor / Slots / Dispatch share one identical frame** (tabs · stat strip · panels), (5) the **old names** POS + KDS return to the rail + prompt (not Line/Pass), (6) the **order/ticket section is redesigned** (check name · covers stepper · Assign table · Dine-in/Takeaway/Delivery · Coursed/All-together · totals · Charge) — and each explores a different visual language for the unified system: **01 Terminal Hairline** (extends the mono command-bar into every surface; flat, hairline-divided), **02 Warm Cards** (soft rounded espresso cards), **03 Segmented Pills** (one pill vocabulary across KPIs/tabs/toggles), **04 Editorial** (big display numbers + uppercase eyebrows, spacious), **05 Dense Console** (high-density always-on pass screen). Standalone; Core dark tokens + Bricolage/Inter/JetBrains-Mono inlined; a fixed "What changed" legend maps the six fixes. Figures illustrative. Open each directly in a browser.

- `sketches/core-pages/*.html` — **the chosen Core redesign ("Dense Glass Console", = `core-redesign-05`) built out to every main Core page.** After the 5-direction round, direction 05 was picked; this set applies its locked style (the near-black liquid-glass aurora, frosted panels, the terminal command bar, the click-to-expand icon Lens Rail with the old **Floor/POS/KDS/Book** names, the unified compact stat strip, SLA tone ramp, and the floating Context Dock) to all eleven operator surfaces, each a standalone file: **01-pos** (icon category rail · open-checks · menu grid · coursed order ticket + cross-sell/combo), **02-kds** (dark kitchen wall · station strip · New/Firing/Pass lanes with cook-meters + SLA tones), **03-orders** (scope tabs · channel filters · orders table · detail dialog), **04-service-floor** (bottleneck banner · zoned table tiles by state), **05-service-slots** (capacity fill bars · demand-exchange levers), **06-service-dispatch** (pass order cards · driver roster + ETAs), **07-guest-inbox** (WhatsApp 3-pane + live order + NBA), **08-guest-crm** (RFM roster table + profile drawer), **09-guest-loyalty** (metal tier gems · members table · family wallet), **10-guest-concierge** (MCP capability toggles · sample JSON · EU-14 allergen matrix), **11-book** (reservations timeline with live conflict hatch + new-reservation panel). Orders + Guest are cross-cutting (no active lens; "reached via ⌘K"). Core dark liquid-glass tokens + Bricolage/Inter/JetBrains-Mono inlined; figures illustrative. Open each directly in a browser.

- `sketches/core-dense-console-suite.html` — **all eleven `core-pages/` surfaces in ONE standalone click-through file** (the chosen "Dense Glass Console" redesign). A slim frosted top switcher (POS · KDS · Orders · Floor · Slots · Dispatch · Inbox · CRM · Loyalty · Concierge · Book, `←`/`→` to flip) swaps the surface below. Built by `sketches/core-pages/build-suite.mjs`, which inlines each page's `<style>` **scoped under a per-page `#pg-*` id** (every selector namespaced, `@keyframes` renamed) so the eleven documents share one file with zero CSS/JS collisions — and because it renders top-level (no iframes) the `backdrop-filter` glass composites correctly. Regenerate after editing any `core-pages/*.html`: `node tests/sketches/core-pages/build-suite.mjs` (from repo root). Open directly in a browser.

## Tests
`tests/*.test.ts` (and `src/lib/*.test.ts`) are the `node:test` suite, run by `npm test` (`tsx --test`) and gated in CI. Most are pure-logic invariants. `bundle-composer.test.ts` is the exception: a **render** test that mounts a real React component in `happy-dom` (`@happy-dom/global-registrator` + `react-dom/client` + `act`) and asserts behaviour through the mount → fetch → render cycle — added after the inline `BundleComposer` shipped stuck-loading / lost-prefill bugs that pure-logic tests can't catch. Use it as the template for future component render tests; keep such files extension `.ts` and build the tree with `React.createElement` so they match the `tests/*.test.ts` glob.
